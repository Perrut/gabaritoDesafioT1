<!DOCTYPE html>
<html>
  <head>
    <title>Teste2</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- A variavel alert pertence a uma hash especial do sistema, chamada
    flash hash, e eh utilizada para exibir avisos sobre o retorno de
    alguma acao realizada por algum controller -->
    <p id="notice"><%= alert %></p>
    <!-- Se o usuario estiver logado, exibe uma mensagem de aviso e um link
    para ele efetuar logout -->
    <% if logged_in? %>
      <p>Você está logado <%= current_user.name %> <%= link_to "(Logout)", logout_path, method: :delete %></p>
    <% end %>
    <%= yield %>
  </body>
</html>
